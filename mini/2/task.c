#include <math.h>
#include <stdio.h>

typedef struct {
    int x, y;
} Point;

? isRightTriangle(Point a, Point b, Point c) {

}

int main() {
    //init points
    Point a;
    Point b;
    Point c;

    ? answer = isRightTriangle(a, b, c);
    //print answer

    return 0;
}